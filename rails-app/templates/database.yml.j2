production:
  adapter: postgresql
  encoding: unicode
  database: {{app_name}}
  username: {{app_name}}
  password: "{{lookup('password', 'credentials/' + app_name + '_password')}}"
  pool: 5
  socket: /var/run/postgresql
